package com.julian.client.mixinext

/** Carries the computed scale factor from extraction time to submit time for a ground item entity. */
interface ItemEntityRenderStateExtension {
    var hugosmpScale: Float
}
